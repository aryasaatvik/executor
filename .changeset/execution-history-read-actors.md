---
"@executor-js/api": minor
"@executor-js/host-mcp": minor
"@executor-js/plugin-execution-history": minor
"@executor-js/sdk": minor
---

Add provider-neutral execution actors and propagate them through HTTP and MCP
execution triggers. Extend execution history with keyset pagination, aggregate
facets, actor filtering, and immutable blob-backed run detail exposed through
an optional HTTP API.
